# Quick Fix Commands
# Paste these into your VS Code PowerShell terminal:

# Step 1 — Install SERVER packages
cd "c:\Projects\AI Interview Platfrom\interviewai\server"
npm install

# Step 2 — Start the server (in this same terminal)
npm run dev

# Step 3 — Open a NEW terminal tab and install CLIENT packages
cd "c:\Projects\AI Interview Platfrom\interviewai\client"
npm install

# Step 4 — Start the client (in the client terminal)
npm run dev
